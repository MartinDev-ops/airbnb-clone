# Base URL of the backend API.
# In local development this is your Express server (see backend/.env.example).
# After deploying the backend to Heroku, set this to that app's URL, e.g.
# https://your-backend-app.herokuapp.com/api
VITE_API_URL=http://localhost:5002/api
